package org.gtk;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.ProgressBar;

public class GtkSpinner extends ProgressBar {

    public GtkSpinner(Context context) {
        super(context);
    }

    public GtkSpinner(Context context, AttributeSet attrs) {
        super(context, attrs);
    }
}
